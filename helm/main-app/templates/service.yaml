apiVersion: v1
kind: Service
metadata:
  name: {{ include "main-app.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "main-app.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}